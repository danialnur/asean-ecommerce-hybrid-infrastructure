# Evidences

Verification screenshots for Phase 1 (Basic Connectivity) and Phase 2 (Dynamic Routing & IPv6). See
[`evidences.md`](evidences.md) for the full, captioned write-up — this file exists only so GitHub renders
something when browsing this folder directly (it doesn't auto-display `evidences.md` the way it does `README.md`).

- [`phase-1-basic-connectivity/`](phase-1-basic-connectivity/) — VLANs, trunking, EtherChannel, HSRP/STP, port
  security, SSH
- [`phase-2-dynamic-routing-ipv6/`](phase-2-dynamic-routing-ipv6/) — OSPF adjacencies/routes, IPv4 reachability,
  IPv6 addressing and routing
