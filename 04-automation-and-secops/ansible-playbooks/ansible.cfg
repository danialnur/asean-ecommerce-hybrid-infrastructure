[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False

[persistent_connection]
; Cisco IOS network_cli connections benefit from a longer timeout than
; Ansible's SSH default - config-mode commands (write memory, large ACL
; pushes) can take longer to return than a typical Linux shell command.
command_timeout = 60
connect_timeout = 60
