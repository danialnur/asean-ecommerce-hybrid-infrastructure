# Threat Simulations — TCP vs UDP Packet Capture

Empty — genuinely outstanding, not a placeholder. `../phase3-plan.md`'s "Packet Tracer Simulation Mode" section
walks through capturing a TCP 3-way handshake (SSH) and a connectionless UDP datagram (Syslog/SNMP) side by side
in Packet Tracer's Simulation Mode, to demonstrate the CCNA 1.5 TCP-vs-UDP contrast concretely rather than just
describing it. Real Wireshark can't see this traffic (it never touches the host's real NIC), so Simulation Mode
is the only way to capture it.

Screenshots go here once captured, following the same numbered/captioned convention as
[`../../01-regional-on-premises-network/evidences/`](../../01-regional-on-premises-network/evidences/).
