# Firewalls & ACLs

Deliberately empty — kept as a placeholder matching the repo structure described in the root `README.md`, not
because this content is missing. ACL policy design (the standard MGMT-SSH-ONLY ACL and the extended
GUEST-CONTAINMENT ACL) is documented in [`../phase3-plan.md`](../phase3-plan.md); the actual ACL configuration
lives inline in every device's `.cfg` file:

- [`../../01-regional-on-premises-network/switching/`](../../01-regional-on-premises-network/switching/)
- [`../../01-regional-on-premises-network/router-configs/`](../../01-regional-on-premises-network/router-configs/)
